# 역설명 Q&A 뱅크

> 사용법: 이동 중에 질문만 보고 **소리 내서(또는 채팅으로) 답해본다.**
> 답을 확인하는 게 아니라 **말로 만들어보는 게** 목적 — 답이 안 나오면 해당 카드로 돌아간다.
> AI 에게 "qa-bank ①번 질문 나한테 물어봐줘, 내 답 검증해줘" 라고 시키면 셀프 테스트가 된다.

## ① 목록 조회 (카드: cards/01-list-query.md)

1. 신청자 이름이 응답에 없던 근본 원인은? ("느슨한 참조"를 설명에 포함할 것)
2. 왜 응답 DTO 를 직접 수정하지 않고 상속 클래스를 만들었나?
3. SQL 조인 대신 배치 조회를 택한 이유 두 가지는? 대신 포기한 것은?
4. "N+1 문제 아니냐"는 질문에 뭐라고 답할 건가?
5. 동명이인 검색이 별도 구현 없이 해결된 이유는?
6. 하위조직 확장에서 원본 검색 DTO 의 organizationId 를 왜 비워야 하나?
   안 비우면 정확히 어떤 일이 벌어지나?
7. `cb.like(path, pattern, '\\')` 의 세 번째 인자는 뭐고, 없으면 뭐가 문제인가?
8. 검색 키워드 매칭이 0명일 때 서비스는 어떻게 동작하나? 왜 그렇게 했나?

## ② 자동발번 (카드: cards/02-auto-numbering.md)

1. "조회해서 +1" 방식의 내장 약점은? (race window 를 설명에 포함할 것)
2. 동시 등록 시 DB 가 알아서 줄 세워주지 않는 이유는? (줄은 세우는데 왜 늦나)
3. 유니크 제약의 역할을 "오염"과 "에러"라는 단어로 한 문장 설명해보라
4. save() 에 @Transactional 을 붙이면 왜 재시도가 죽은 코드가 되나? ★역설명 통과(8/8)
5. 동료: "DB 시퀀스 쓰면 되잖아요?" — 뭐라고 답할 건가
6. 3회 연속 충돌하면 사용자는 뭘 보게 되나

## 깨보기 실습 아이디어 (책상에서)

- [ ] escapeLike 호출을 지우면 어떤 테스트가 깨질까 → 예측 후 실행
- [ ] setOrganizationId(null) 줄을 지우면? (테스트가 부수효과를 검증하는지 확인)
- [ ] 발번 재시도 횟수를 0으로 바꾸면 어떤 테스트가 깨질까
